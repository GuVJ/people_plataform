// RGB triplet for --color-primary (#2563EB) — for the handful of spots that need to compute
// an alpha-blended color in JS (heatmaps, intensity grids) where a CSS variable can't be used.
export const PRIMARY_RGB = '37, 99, 235';

// Amber/orange triplet for "attention" heat scales (ex.: gradiente de banco de horas),
// onde o maior valor deve chamar mais atenção que o menor.
export const HEAT_RGB = '234, 88, 12';
